#include <stdio.h>

/**
 * main - prints exactly "Programming is like building a multilingual puzzle
 * followed by a new line
 * Return: 0
 */
int main(void)
{
	char std[] = "\"Programming is like building a multilingual puzzle";
	puts(std);
	return 0;
}
